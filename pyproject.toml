[project]
name = "cockpit-qemu"
version = "0.1.0"
description = "Debianマシンにcockpitとqemu-ppc仮想マシンをセットアップします"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pyinfra>=3.6",
    "python-dotenv>=1.2.1",
]
